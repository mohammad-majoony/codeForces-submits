#include<bits/stdc++.h>
#define MAJOONI ios_base::sync_with_stdio(0) ; cin.tie(0) ;
typedef long long ll ;
using namespace std ;

void solve(){
    ll a , b , c , d ; 
    cin >> a >> b >> c >> d ;
    cout << b << " " << c << " " << c << '\n' ;
}       

int main(){
    MAJOONI
    int tc ; 
    cin >> tc ; 
    while(tc--) solve();
}