#include<bits/stdc++.h>
using namespace std ;

void solve(){
    int len ;
    cin >> len ;
    while(len--) cout << 1 << " " ;
    cout << '\n' ;
}

int main(){
    ios_base::sync_with_stdio(0) ;
    cin.tie(0) ;

    int tc ; cin >> tc ;
    while(tc--) solve() ;
}